const Dashboard = () => {
    return (
      <div>
        <h2>User Dashboard</h2>
        <p>Welcome, User! You can access your profile and settings here.</p>
        {/* You can add user-specific features like viewing profile, settings, etc. */}
      </div>
    );
  };

export default Dashboard;
